diff -ruN a/src/main/ipc/register.ts b/src/main/ipc/register.ts
--- a/src/main/ipc/register.ts
+++ b/src/main/ipc/register.ts
@@ -2412,11 +2412,6 @@
   ipcMain.handle("list-models", () => {
     const conn = getConnectionConfig();
     if (conn.mode === "remote") {
-      if (conn.remoteChatTransport === "legacy") {
-        throw new Error(
-          "Remote model library reads require dashboard transport.",
-        );
-      }
       return remoteListModels(conn);
     }
     if (conn.mode === "ssh" && conn.ssh) {
diff -ruN a/src/main/remote-sessions.ts b/src/main/remote-sessions.ts
--- a/src/main/remote-sessions.ts
+++ b/src/main/remote-sessions.ts
@@ -102,6 +102,7 @@
         method: options.method ?? "GET",
         headers: {
           "Content-Type": "application/json",
+          Authorization: `Bearer ${token}`,
           "X-Hermes-Session-Token": token,
           ...(body ? { "Content-Length": Buffer.byteLength(body) } : {}),
         },
diff -ruN a/src/main/ssh-remote.ts b/src/main/ssh-remote.ts
--- a/src/main/ssh-remote.ts
+++ b/src/main/ssh-remote.ts
@@ -37,8 +37,14 @@
 
 // ── SSH exec core ────────────────────────────────────────────────────────────
 
+function resolveIdentityPath(keyPath: string | undefined): string | null {
+  const value = keyPath?.trim();
+  if (!value) return null;
+  return value.startsWith("~/") ? join(homedir(), value.slice(2)) : value;
+}
+
 function buildExecArgs(config: SshConfig): string[] {
-  const keyPath = config.keyPath?.trim() || join(homedir(), ".ssh", "id_rsa");
+  const keyPath = resolveIdentityPath(config.keyPath);
   return [
     "-o",
     "BatchMode=yes",
@@ -47,8 +53,7 @@
     "-o",
     "ConnectTimeout=15",
     ...buildSshControlOptions(),
-    "-i",
-    keyPath,
+    ...(keyPath ? ["-i", keyPath] : []),
     "-p",
     String(config.port || 22),
     `${config.username}@${config.host}`,
@@ -62,9 +67,9 @@
   timeoutMs = 30000,
 ): Promise<string> {
   return new Promise((resolve, reject) => {
-    const keyPath = config.keyPath?.trim() || join(homedir(), ".ssh", "id_rsa");
-    if (!existsSync(keyPath)) {
-      return reject(new Error(`SSH private key file not found at: ${keyPath}`));
+    const keyPath = resolveIdentityPath(config.keyPath);
+    if (keyPath && !existsSync(keyPath)) {
+      return reject(new Error(`SSH identity file not found at: ${keyPath}`));
     }
 
     const child = spawn("ssh", [...buildExecArgs(config), command], {
@@ -930,17 +935,64 @@
 
 // ── Env / Config (Providers) ─────────────────────────────────────────────────
 
-function remoteEnvPath(profile?: string): string {
+const remoteHermesHomeCache = new Map<string, Promise<string>>();
+
+function sshTargetKey(config: SshConfig): string {
+  return `${config.host}:${config.port || 22}:${config.username}`;
+}
+
+async function sshResolveHermesHome(config: SshConfig): Promise<string> {
+  const cacheKey = sshTargetKey(config);
+  const inflight = remoteHermesHomeCache.get(cacheKey);
+  if (inflight) return inflight;
+
+  const run = (async (): Promise<string> => {
+    try {
+      // Prefer the managed systemd unit's HERMES_HOME when present. Plain
+      // installs still fall back to ~/.hermes.
+      const script =
+        'for unit in hermes-agent.service hermes.service; do ' +
+        'systemctl show "$unit" -p Environment --value 2>/dev/null | ' +
+        "tr ' ' '\\n' | sed -n 's/^HERMES_HOME=//p' | head -n1; done";
+      const out = await sshExec(config, `sh -c ${shellQuote(script)}`);
+      const home = out
+        .split(String.fromCharCode(10))
+        .map((line) => line.trim())
+        .find(Boolean);
+      if (home) return home;
+    } catch {
+      // fall through
+    }
+    return "~/.hermes";
+  })();
+
+  remoteHermesHomeCache.set(cacheKey, run);
+  try {
+    return await run;
+  } catch (error) {
+    remoteHermesHomeCache.delete(cacheKey);
+    throw error;
+  }
+}
+
+async function remoteEnvPath(
+  config: SshConfig,
+  profile?: string,
+): Promise<string> {
+  const home = await sshResolveHermesHome(config);
   if (profile && profile !== "default")
-    return `~/.hermes/profiles/${profile}/.env`;
-  return "~/.hermes/.env";
+    return `${home}/profiles/${profile}/.env`;
+  return `${home}/.env`;
 }
 
 export async function sshReadEnv(
   config: SshConfig,
   profile?: string,
 ): Promise<Record<string, string>> {
-  const content = await sshReadFile(config, remoteEnvPath(profile));
+  const content = await sshReadFile(
+    config,
+    await remoteEnvPath(config, profile),
+  );
   const result: Record<string, string> = {};
   for (const line of content.split("\n")) {
     const trimmed = line.trim();
@@ -1010,7 +1062,7 @@
   value: string,
   profile?: string,
 ): Promise<void> {
-  const envPath = remoteEnvPath(profile);
+  const envPath = await remoteEnvPath(config, profile);
   const content = await sshReadFile(config, envPath);
   await sshWriteFile(config, envPath, upsertEnvLine(content, key, value));
 }
@@ -2444,7 +2496,7 @@
   config: SshConfig,
   profile?: string,
 ): Promise<string> {
-  const envPath = remoteEnvPath(profile);
+  const envPath = await remoteEnvPath(config, profile);
   const cacheKey = `${config.host}:${config.port || 22}:${config.username}:${envPath}`;
   const inflight = dashboardTokenPromises.get(cacheKey);
   if (inflight) return inflight;
@@ -2522,7 +2574,7 @@
   profile: string | undefined,
   port: number,
 ): Promise<void> {
-  const envPath = remoteEnvPath(profile);
+  const envPath = await remoteEnvPath(config, profile);
   // Write exactly ONE canonical line (strip any prior entries, then append) so
   // repeated port reallocations don't accumulate duplicate
   // HERMES_DESKTOP_DASHBOARD_PORT lines — sshReadEnv is last-wins, so dupes
diff -ruN a/src/main/ssh-tunnel.ts b/src/main/ssh-tunnel.ts
--- a/src/main/ssh-tunnel.ts
+++ b/src/main/ssh-tunnel.ts
@@ -97,16 +97,21 @@
   });
 }
 
+function resolveIdentityPath(keyPath: string | undefined): string | null {
+  const value = keyPath?.trim();
+  if (!value) return null;
+  return value.startsWith("~/") ? join(homedir(), value.slice(2)) : value;
+}
+
 function buildSshArgs(config: SshConfig, localPort: number): string[] {
-  const keyPath = config.keyPath || join(homedir(), ".ssh", "id_rsa");
+  const keyPath = resolveIdentityPath(config.keyPath);
   return [
     "-N",
     "-L",
     `${localPort}:127.0.0.1:${config.remotePort}`,
     "-p",
     String(config.port),
-    "-i",
-    keyPath,
+    ...(keyPath ? ["-i", keyPath] : []),
     "-o",
     "StrictHostKeyChecking=accept-new",
     "-o",
@@ -127,7 +132,7 @@
     config.host,
     config.port || 22,
     config.username,
-    config.keyPath || join(homedir(), ".ssh", "id_rsa"),
+    resolveIdentityPath(config.keyPath) ?? "<open-ssh-config>",
     config.remotePort,
   ].join("\n");
 }
@@ -136,9 +141,9 @@
   stopSshTunnel();
   const generation = ++tunnelGeneration;
 
-  const keyPath = config.keyPath?.trim() || join(homedir(), ".ssh", "id_rsa");
-  if (!existsSync(keyPath)) {
-    throw new Error(`SSH private key file not found at: ${keyPath}`);
+  const keyPath = resolveIdentityPath(config.keyPath);
+  if (keyPath && !existsSync(keyPath)) {
+    throw new Error(`SSH identity file not found at: ${keyPath}`);
   }
 
   const localPort = await findFreePort(config.localPort || 18642);
@@ -273,6 +278,12 @@
 
 // Test SSH reachability + hermes health endpoint through a temporary tunnel
 export function testSshConnection(config: SshConfig): Promise<boolean> {
+  const keyPath = resolveIdentityPath(config.keyPath);
+  if (keyPath && !existsSync(keyPath)) {
+    return Promise.reject(
+      new Error(`SSH identity file not found at: ${keyPath}`),
+    );
+  }
   return findFreePort(config.localPort || 19642)
     .then(
       (localPort) =>
diff -ruN a/src/renderer/src/components/settings/useSettingsData.ts b/src/renderer/src/components/settings/useSettingsData.ts
--- a/src/renderer/src/components/settings/useSettingsData.ts
+++ b/src/renderer/src/components/settings/useSettingsData.ts
@@ -547,17 +547,22 @@
       }
       setConnTesting(true);
       setConnStatus(null);
-      const ok = await window.hermesAPI.testSshConnection(
-        sshHost.trim(),
-        parseInt(sshPort, 10) || 22,
-        sshUser.trim(),
-        sshKeyPath.trim(),
-        parseInt(sshRemotePort, 10) || 8642,
-      );
-      setConnTesting(false);
-      setConnStatus(
-        ok ? t("settings.sshSuccess") : t("settings.sshErrorFailedSimple"),
-      );
+      try {
+        const ok = await window.hermesAPI.testSshConnection(
+          sshHost.trim(),
+          parseInt(sshPort, 10) || 22,
+          sshUser.trim(),
+          sshKeyPath.trim(),
+          parseInt(sshRemotePort, 10) || 8642,
+        );
+        setConnStatus(
+          ok ? t("settings.sshSuccess") : t("settings.sshErrorFailedSimple"),
+        );
+      } catch (error) {
+        setConnStatus(error instanceof Error ? error.message : String(error));
+      } finally {
+        setConnTesting(false);
+      }
     } else {
       const url = connRemoteUrl.trim();
       if (!url) {
diff -ruN a/src/shared/i18n/locales/en/settings.ts b/src/shared/i18n/locales/en/settings.ts
--- a/src/shared/i18n/locales/en/settings.ts
+++ b/src/shared/i18n/locales/en/settings.ts
@@ -210,8 +210,9 @@
   sshHost: "SSH Host",
   sshPort: "SSH Port",
   sshUsername: "Username",
-  sshKeyPath: "Private Key Path",
-  sshKeyPathOptional: "(optional, defaults to ~/.ssh/id_rsa)",
+  sshKeyPath: "Identity File",
+  sshKeyPathOptional:
+    "(optional; blank uses OpenSSH config and SSH_AUTH_SOCK; public .pub files are supported)",
   sshRemotePort: "Remote Hermes Port",
   sshRemotePortDefault: "(default 8642)",
   sshHint:
